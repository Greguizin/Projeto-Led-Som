Devera criar um arquivo chamado .env para as informações do DB um exemplo abaixo:

    .env:
        DB_HOST=localhost
        DB_USER=postgres
        DB_PASS=123
        DB_NAME=teste
        DB_PORT=5432
